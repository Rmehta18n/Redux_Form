import { StyleSheet } from "react-native";
import { COLORS } from "../../constants";

const appTextInputStyles = StyleSheet.create({
    textInputStyle: {
        backgroundColor: COLORS.white,
        borderRadius: 5,
        fontSize: 16,
        elevation: 5,
        padding: 10,

    },

})
export { appTextInputStyles }